name: "Terraform"

on: [push, pull_request]

jobs: 
    Terraform-Docs:
        uses: Dielvic/iac-aks/.github/.github/workflows/terraform-docs.yml@feature1
        if: github.event_name == 'pull_request'
        with:
            os_version: "ubuntu-latest"
    Terraform:
        uses: Dielvic/iac-aks.github/.github/workflows/reusable-terraform.yml@feature1
        with:
            os_version: "ubuntu-latest"
            environment: "prod"
        secrets:
            ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
            ARM_CLIENT_SECRET: ${{ secrets.ARM_CLIENT_SECRET }}
            ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID }}
